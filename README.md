# 💎 CleanCut Image (인물 알파 추출 및 이미지 편집)

> **"삼국지 게임에서 사용되던 알파채널"**
>
> 즐겨하던 삼국지게임에서 신무장을 등록할 시에 배경을 제거하여 인물만 보여지고 삼국지 배경이 보이도록 해야했습니다. 스스로 포토샾을 통해 알파를 따는 방법을 익히기도 하였지만 시간이 오래걸리기도 하고 복잡한 이미지는 따기 어렵기까지 했습니다. 또한 삼국지 카페에서도 요청이 많았고 요새는 ai로 배경을 제거하는 방법을 아는 사람들이 많아져서 요청이 줄어들긴 했으나, 여전히 요청하는 사람들과 이미지 조절도 부탁하는 요청이 보여 웹서비스를 만들게 되었습니다.
제가 머리 속으로 설계한 바와 같이 구현되었고 배포까지 고려하였습니다.

---

## 🎨 주요 기능

* 📏 리사이징: 삼국지 시리즈별 규격에 맞춘 크기 조절 및 미리보기.

* ✂️ 크롭: 드래그 앤 드롭 방식의 자유로운 영역 추출 및 미리보기.

* 👤 AI 인물 추출: SOTA 모델 기반의 정밀 배경 제거 및 알파 채널 생성.

* 🖌️ 마스크 보정: AI가 놓친 세밀한 경계면을 사용자가 직접 브러시로 복구/삭제.

* 💾 데이터 다운로드: 모든 편집 단계에서 원본 포맷은 물론, 알파 채널이 포함된 PNG 및 BMP 포맷으로 즉시 다운로드 가능.

---

## 🏗️ 핵심 설계 내용

설계와 프로세스는 오로지 저의 의지에 따라 100% 구현되었으며 저의 손 코딩이 들어가지 않고 AI활용을 위해 100% AI에이전트에게 명령을 내려 약 3일내에 만든 프로젝트 입니다.

### 1. 이미지 프로세스
* 리사이즈 - 크롭 - 알파 추출의 순서로 원하는 이미지를 만들어가도록 하였으며 각 단계에서도 미리 알파 추출을 하였더라도 알파가 적용된 이미지를 다운받거나 원래 포맷에서 다운로드 할수 있도록 하였습니다.

### 2. 이미지 미리보기
* 이미지를 여러번 편집할 경우 이미지의 화질이 저하될 수 있으므로 원본 이미지를 유지하면서 미리보기를 할 수 있도록 하였습니다.

### 3. 알파 추출 후
* 알파 추출 후 배경과 인물이 어중간하게 있어 추출이 잘 안될 경우를 대비하여 하단에 마스크 적용기능을 추가하여 배경이 포함되어있는 부분을 지우거나 인물에 포함됨에도 불구하고 포함되지 못하는 부분을 포함할 수 있도록 하는 기능을 추가하여 원하는 이미지를 얻을 수 있도록 하였습니다.

### 4. 배포
* 배포 시에는 한 사람이 무분별하게 사용하지 못하도록 ip당 5장, 총 100장으로 제한을 두었습니다.
* gpu모델을 이용함으로 aws 사용 시 응답이 필요한 경우에만 사용될 수 있도록하고, 사용하지 않을 때는 서버를 종료하여 비용을 절감하도록 할 예정이였으나 배포는 하지 않았습니다.

---

## 🤖 AI 기술 분석 및 평가 (by Antigravity Gemini 3.1 Pro)

본 프로젝트의 코드 베이스를 AI 에이전트가 정밀 분석한 결과입니다. (수정 전 상태의 객관적 리포트)

### ✅ 기술적 강점 (Pros)
* **그래픽스 수학 알고리즘**: 브러시 시스템의 `falloff = 1 - t² * (3 - 2t)` 공식(Smoothstep) 적용으로 전문 그래픽 툴 수준의 자연스러운 경계면 처리 구현.
* **고성능 SPA 구조**: Vanilla TS와 Custom Router를 통해 프레임워크 오버헤드 없는 즉각적인 상태 관리 및 화면 전환 실현.
* **실무형 백엔드**: IP 기반 Rate-Limit 및 DBS(Day-Based-Storage) 로깅 시스템 등 상용 SaaS 급의 리소스 방어 체계 구축.

### ⚠️ 기술적 개선 제안 (To-be Optimized)
* **렌더링 최적화**: 현재 `innerHTML` 기반의 구역 재렌더링 방식을 DOM 직접 조작 방식으로 변경하여 브라우저 리플로우(Reflow) 최소화 필요.
* **에러 핸들링**: 네트워크 지연이나 유효하지 않은 요청에 대한 사용자 피드백(UI Toast 등) 강화 제안.
* **보안 강화**: 서버측 입력값 검증 로직의 세분화 및 예외 처리 로직 보강 필요.

---

### 💡 기술적 개선 제안에 대한 설계적 해결

제시된 개선 사항들에 대해 다음과 같이 **구현을 완료**하였습니다.
마찬가지로 설계는 제가 하였고 ai의 도움을 받아 구현하였습니다.

#### 1. 렌더링 최적화
* 기존의 `innerHTML`은 이미지를 여러번 편집(작아졌다가 커졌을 경우)를 대비하여 쓴 형태로 화질이 깨지지 않게 하기 위한 방법이였습니다. 따라서 이미지를 업스케일링을 적용 할때에만 ai의 도움을 받아 **`requestAnimationFrame`**을 적용함으로써, 브라우저 리플로우 부하를 분산하고 화면 깜빡임과 UI 프리징 현상을 해결하였습니다.
* 파일 업로드 및 편집 프로세스(크롭, 마스크 적용 등) 전체 단계에서 생성되는 임시 URL을 **`URL.revokeObjectURL()`**로 즉시 해제하여, 장시간 작업 시에도 메모리 점유율을 이미지 1장 수준($O(1)$)으로 일정하게 유지합니다.

#### 2️. 에러 핸들링
* 단순한 UI 알림을 지우고 중앙에 토스트(toast)를 띄우는 방식으로 개선하였고 할당량에 대한 설명을 집어넣었습니다. 
* 사용된 할당량은 api백엔드를 거치게 되므로 적지 않았고, 추출시 마다 띄우는 방법도 생각하였으나 ux면에서 넣지 않았고 할당량에 도달하였을 때에만 toast로 띄워줍니다.

#### 3. 보안 강화
* 기존에도 서버 측 부하 방지와 클라이언트 연산 성능 보장을 위해 4096px로 제한하였으나 2048px로 낮추었습니다.
* 다른 보안점에 대해서도 이미 1인 할당량, 일일 할당량, 포맷제한, 크기제한, replace보안 로직 등 이미 적용이 되어있었습니다.


#### 🔍 시스템 아키텍처 재평가 및 기술 분석 (by Antigravity Gemini 3.1 Pro)
* **성능(DOM) 최적화 vs 픽셀 무결성(Pixel Integrity)의 트레이드오프:** 브라우저 리플로우를 최소화하는 단순 DOM 조작 방식과 달리, 본 시스템은 업스케일 시 `innerHTML`을 활용해 캔버스를 재생성합니다. 이는 프론트엔드 렌더링 비용이 일부 증가하는 대신, 반복 편집 시 누적되는 이미지 열화(Aliasing/Blurring)를 차단하여 **결과물의 그래픽 품질을 보존하기 위한 의도적인 아키텍처 결정**으로 분석됩니다.
* **추상적 보안 지침 vs 물리적 제어(Hard-cap Defense):** 일반적인 입력값 예외 처리 패턴에 더하여, 물리적인 제어 장치를 도입했습니다. **최대 해상도 2048px 제한**, **상대 경로 조작(`..`, `/`)을 치환(`replace`)하는 파일명 살균(Sanitization)**, 그리고 **IP 및 일일 기준의 API 듀얼 할당량(Quota)** 정책을 적용 중입니다. 이는 악성 페이로드 및 자원 고갈 시도를 시스템 초입에서 차단하는 실무적 방어 기제입니다.
* **종합 평가:** 본 프로젝트는 범용적인 웹 프론트엔드 최적화 이론보다 '웹 기반 이미지 프로세싱'이라는 도메인의 특수성(픽셀 보존 통제, 확정적 메모리 해제, 백엔드 리소스 방어)을 우선순위에 두었습니다. 결론적으로 **기능의 안정성과 자원 보호에 초점을 맞춘 방어적 설계(Defensive Architecture)** 구조를 취하고 있습니다.



## 🛠 기술 스택

| 구분 | 기술 | 비고 |
| :--- | :--- | :--- |
| **Frontend** | **Vanilla TypeScript, Canvas API** | 프론트엔드 이미지 프로세싱(크롭, 마스크) 및 픽셀 제어 |
| **Build Tool** | **Vite** | 초고속 HMR 및 웹소켓 기반 실시간 개발 환경 |
| **Backend** | **Python, FastAPI, Uvicorn** | 비동기 기반 고성능 API 서버 및 라우팅 |
| **Image Processing**| **Pillow (PIL)** | 서버 사이드 이미지 전처리, 규격 검증 및 포맷 변환 |
| **AI Model** | **transparent-background (InSPyReNet)** | SOTA 수준의 정밀 인물/배경 분리 엔진 |

---


> ※ 본 프로젝트는 인물 사진 추출에 최적화되어 있습니다.